#include <stdio.h>
#include <unistd.h>

/**
 * main - Write a program that prints "$ ", wait for the user to enter command,
 * * andprints it on the next line.
 * @ac:
 * @av:
 * Return: 0
 */

int main(int ac, char **av)
{
while(1)
{
printf("$ ");
}
return (0);
}
